{
  "name": "backend-distributed-system-practices",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "bun src/server.ts",
    "test": "bun test",
    "db:new": "bun run scripts/new-migration.ts",
    "db:up": "bun run scripts/migrate.ts up",
    "db:down": "bun run scripts/migrate.ts down",
    "db:status": "bun run scripts/migrate.ts status"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
